<% content_for(:title, @schedule_spot.snapshot_name) %>
<% breadcrumb :schedule_spot, @schedule_spot %>

<%= render 'shared/schedule_spots/schedule_spot_details',
    authorization_scope: current_user,
    back_link_path: schedule_path(@schedule)
%>
