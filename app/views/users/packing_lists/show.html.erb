<%# 個人用（/packing_list）としおり用（/schedules/:id/packing_list）の切り替えはコントローラーのhelper_methodで処理 %>
<% content_for(:title, t('packing_lists.show.title')) %>
<% if @schedule.present? %>
  <% breadcrumb :schedule_packing_list, @schedule %>
<% end %>

<%= render "shared/packing_lists/show" %>
